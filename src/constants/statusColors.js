import Tag from 'components/Tag';
import { SHORTCUT_STATE_DRAFT } from 'constants/shortcutStates';
import { VERSION_STATE_DRAFT } from 'constants/versionStates';

export const SHORTCUT_STATUS_COLORS = {
  [SHORTCUT_STATE_DRAFT]: Tag.Colors.Blue
}

export const VERSION_STATUS_COLORS = {
  [VERSION_STATE_DRAFT]: Tag.Colors.Blue
}